[package]
name = "grove"
version = "0.1.0"
edition = "2024"

[dependencies]
nannou = { path = "/home/ben/Projects/personal/repos/nannou/nannou" }
bytemuck = { version = "1", features = ["derive"] }

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = true
